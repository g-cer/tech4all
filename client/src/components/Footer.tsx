import React from "react";
import styles from "../css/Footer.module.css";

const Footer: React.FC = () => (
  <footer className={styles.footer}>
    <p>Tech4All — Byte per Byte, verso il futuro.</p>
  </footer>
);

export default Footer;
